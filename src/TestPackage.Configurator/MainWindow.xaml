<Window x:Class="TestPackage.Configurator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestPackage Configurator" Width="900"
        MaxHeight="800" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="TestPackage Configurator" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Design and generate simulated installers for testing" FontSize="11" Foreground="#CCE5FF" Margin="0,2,0,0"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="v2.0.0" Foreground="#99CCFF" FontSize="11" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Content: Output Settings + Tabs -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,10,20,10">

                <!-- Output Settings (always visible) -->
                <Border Background="White" CornerRadius="5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Installer EXE Name"/>
                        <TextBox Grid.Column="0" Grid.Row="1" x:Name="TxtInstallerExeName" Style="{StaticResource FieldTextBox}" Text="YourSimulatedSetup.exe" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="1" Style="{StaticResource FieldLabel}" Text="Application EXE Name"/>
                        <TextBox Grid.Column="1" Grid.Row="1" x:Name="TxtAppExeName" Style="{StaticResource FieldTextBox}" Text="YourSimulatedApp.exe" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="2" Style="{StaticResource FieldLabel}" Text="Output Folder"/>
                        <Grid Grid.Column="2" Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtOutputFolder" Style="{StaticResource FieldTextBox}" IsReadOnly="True"/>
                            <Button Grid.Column="1" Content="..." Padding="8,2" Margin="4,0,0,2" Click="BrowseOutput_Click"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Tab Control -->
                <TabControl x:Name="ConfigTabs">

                    <!-- Tab 1: General -->
                    <TabItem Header="General">
                        <StackPanel Margin="15">
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Application Metadata"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Application Name"/>
                            <TextBox x:Name="TxtAppName" Style="{StaticResource FieldTextBox}"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Version"/>
                            <TextBox x:Name="TxtAppVersion" Style="{StaticResource FieldTextBox}"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Publisher"/>
                            <TextBox x:Name="TxtAppPublisher" Style="{StaticResource FieldTextBox}"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="URL"/>
                            <TextBox x:Name="TxtAppURL" Style="{StaticResource FieldTextBox}"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="GUID"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtAppGUID" Style="{StaticResource FieldTextBox}"/>
                                <Button Grid.Column="1" Content="Generate New" Padding="8,2" Margin="4,0,0,0" Click="GenerateGUID_Click"/>
                            </Grid>
                            <CheckBox x:Name="ChkRequireAdmin" Style="{StaticResource FeatureToggle}" Content="Require Administrator (UAC prompt)"/>
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Install Context"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Default Context"/>
                            <ComboBox x:Name="CboDefaultContext" FontSize="12" Margin="0,0,0,2">
                                <ComboBoxItem Content="Machine"/>
                                <ComboBoxItem Content="User"/>
                            </ComboBox>
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Target Directory"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Default Install Path"/>
                            <TextBox x:Name="TxtDefaultPath" Style="{StaticResource FieldTextBox}"/>
                            <CheckBox x:Name="ChkAllowCustomPath" Style="{StaticResource FeatureToggle}" Content="Allow user to customize path"/>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 2: Wizard Pages -->
                    <TabItem Header="Wizard Pages">
                        <StackPanel Margin="15">
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Show/Hide Wizard Pages"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Toggle which pages appear in the simulated installer wizard." FontStyle="Italic"/>
                            <CheckBox x:Name="ChkShowWelcome" Style="{StaticResource FeatureToggle}" Content="Welcome Page"/>
                            <CheckBox x:Name="ChkShowEULA" Style="{StaticResource FeatureToggle}" Content="License Agreement (EULA)"/>
                            <CheckBox x:Name="ChkShowInstallContext" Style="{StaticResource FeatureToggle}" Content="Install Context (Machine/User)"/>
                            <CheckBox x:Name="ChkShowTargetDirectory" Style="{StaticResource FeatureToggle}" Content="Target Directory"/>
                            <CheckBox x:Name="ChkShowComponents" Style="{StaticResource FeatureToggle}" Content="Component Selection"/>
                            <CheckBox x:Name="ChkShowDesktopShortcut" Style="{StaticResource FeatureToggle}" Content="Desktop Shortcut Option"/>
                            <CheckBox x:Name="ChkShowStartMenuPin" Style="{StaticResource FeatureToggle}" Content="Start Menu Pin Option"/>
                            <CheckBox x:Name="ChkShowRebootOption" Style="{StaticResource FeatureToggle}" Content="Reboot Option"/>
                            <CheckBox x:Name="ChkShowActiveSetup" Style="{StaticResource FeatureToggle}" Content="Active Setup Option"/>
                            <TextBlock Style="{StaticResource SectionLabel}" Text="EULA Text"/>
                            <TextBox x:Name="TxtEULAText" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 3: Components -->
                    <TabItem Header="Components">
                        <StackPanel Margin="15">
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Optional Components"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Components create subdirectories and test files. Users select them during installation." FontStyle="Italic"/>
                            <StackPanel x:Name="ComponentsList" Margin="0,8,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBox x:Name="TxtNewComponent" Style="{StaticResource FieldTextBox}" Width="200" Margin="0,0,8,0"/>
                                <Button Content="Add Component" Padding="8,2" Click="AddComponent_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 4: System Actions -->
                    <TabItem Header="System Actions">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="500">
                            <StackPanel Margin="15">
                                <TextBlock Style="{StaticResource SectionLabel}" Text="Test Files"/>
                                <CheckBox x:Name="ChkTestFilesEnabled" Style="{StaticResource FeatureToggle}" Content="Create test files during installation"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Files (path|content, comma-separated)"/>
                                <TextBox x:Name="TxtTestFiles" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" Height="60" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Registry Entries"/>
                                <CheckBox x:Name="ChkRegistryEnabled" Style="{StaticResource FeatureToggle}" Content="Write test registry entries"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Entries (RootKey\Path|Name|Type|Data, comma-separated)"/>
                                <TextBox x:Name="TxtRegistryEntries" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" Height="60" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Shortcuts"/>
                                <CheckBox x:Name="ChkDesktopShortcut" Style="{StaticResource FeatureToggle}" Content="Create desktop shortcut"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Shortcut Name"/>
                                <TextBox x:Name="TxtDesktopShortcutName" Style="{StaticResource FieldTextBox}"/>
                                <CheckBox x:Name="ChkStartMenuEntry" Style="{StaticResource FeatureToggle}" Content="Create Start Menu entry"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Start Menu Folder"/>
                                <TextBox x:Name="TxtStartMenuFolder" Style="{StaticResource FieldTextBox}"/>
                                <CheckBox x:Name="ChkPinToStartMenu" Style="{StaticResource FeatureToggle}" Content="Pin to Start Menu"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="File Associations"/>
                                <CheckBox x:Name="ChkFileAssociations" Style="{StaticResource FeatureToggle}" Content="Register file type associations"/>
                                <TextBox x:Name="TxtFileAssociations" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" Height="40" VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Context Menu"/>
                                <CheckBox x:Name="ChkContextMenu" Style="{StaticResource FeatureToggle}" Content="Add shell context menu entries"/>
                                <TextBox x:Name="TxtContextMenuEntries" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" Height="40" VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Environment Variables"/>
                                <CheckBox x:Name="ChkEnvVars" Style="{StaticResource FeatureToggle}" Content="Set environment variables"/>
                                <TextBox x:Name="TxtEnvVars" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" Height="40" VerticalScrollBarVisibility="Auto"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Tab 5: Advanced Features -->
                    <TabItem Header="Advanced">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="500">
                            <StackPanel Margin="15">
                                <TextBlock Style="{StaticResource SectionLabel}" Text="Windows Service"/>
                                <CheckBox x:Name="ChkService" Style="{StaticResource FeatureToggle}" Content="Install a Windows service (requires admin)"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Service Name"/>
                                <TextBox x:Name="TxtServiceName" Style="{StaticResource FieldTextBox}"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Display Name"/>
                                <TextBox x:Name="TxtServiceDisplayName" Style="{StaticResource FieldTextBox}"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Start Type"/>
                                <ComboBox x:Name="CboServiceStartType" FontSize="12" Margin="0,0,0,2">
                                    <ComboBoxItem Content="Automatic"/>
                                    <ComboBoxItem Content="Manual" IsSelected="True"/>
                                    <ComboBoxItem Content="Disabled"/>
                                </ComboBox>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Scheduled Task"/>
                                <CheckBox x:Name="ChkScheduledTask" Style="{StaticResource FeatureToggle}" Content="Create a scheduled task"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Task Name"/>
                                <TextBox x:Name="TxtTaskName" Style="{StaticResource FieldTextBox}"/>
                                <TextBlock Style="{StaticResource FieldLabel}" Text="Schedule"/>
                                <ComboBox x:Name="CboTaskSchedule" FontSize="12" Margin="0,0,0,2">
                                    <ComboBoxItem Content="Daily" IsSelected="True"/>
                                    <ComboBoxItem Content="Weekly"/>
                                    <ComboBoxItem Content="AtLogon"/>
                                    <ComboBoxItem Content="AtStartup"/>
                                </ComboBox>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Firewall Rules"/>
                                <CheckBox x:Name="ChkFirewall" Style="{StaticResource FeatureToggle}" Content="Create firewall rules (requires admin)"/>
                                <TextBox x:Name="TxtFirewallRules" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap" Height="40" VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Protocol Handlers"/>
                                <CheckBox x:Name="ChkProtocolHandlers" Style="{StaticResource FeatureToggle}" Content="Register custom URI protocol handlers"/>
                                <TextBox x:Name="TxtProtocolHandlers" Style="{StaticResource FieldTextBox}"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Active Setup"/>
                                <CheckBox x:Name="ChkActiveSetup" Style="{StaticResource FeatureToggle}" Content="Register Active Setup component"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="App Paths"/>
                                <CheckBox x:Name="ChkAppPaths" Style="{StaticResource FeatureToggle}" Content="Register in App Paths (Run dialog)"/>

                                <TextBlock Style="{StaticResource SectionLabel}" Text="Startup"/>
                                <CheckBox x:Name="ChkStartup" Style="{StaticResource FeatureToggle}" Content="Add to Windows startup"/>
                                <ComboBox x:Name="CboStartupMethod" FontSize="12" Margin="0,0,0,2">
                                    <ComboBoxItem Content="Registry" IsSelected="True"/>
                                    <ComboBoxItem Content="StartupFolder"/>
                                </ComboBox>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- Tab 6: Uninstall & Reboot -->
                    <TabItem Header="Uninstall &amp; Reboot">
                        <StackPanel Margin="15">
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Uninstall Behavior"/>
                            <CheckBox x:Name="ChkRegisterUninstaller" Style="{StaticResource FeatureToggle}" Content="Register in Add/Remove Programs"/>
                            <CheckBox x:Name="ChkCleanFiles" Style="{StaticResource FeatureToggle}" Content="Clean files on uninstall"/>
                            <CheckBox x:Name="ChkCleanRegistry" Style="{StaticResource FeatureToggle}" Content="Clean registry on uninstall"/>
                            <CheckBox x:Name="ChkCleanShortcuts" Style="{StaticResource FeatureToggle}" Content="Clean shortcuts on uninstall"/>

                            <TextBlock Style="{StaticResource SectionLabel}" Text="Intentional Leftovers (for testing incomplete uninstall detection)"/>
                            <CheckBox x:Name="ChkLeaveFiles" Style="{StaticResource FeatureToggle}" Content="Leave files behind after uninstall"/>
                            <TextBox x:Name="TxtLeftoverFiles" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap"/>
                            <CheckBox x:Name="ChkLeaveRegistry" Style="{StaticResource FeatureToggle}" Content="Leave registry entries behind after uninstall"/>
                            <TextBox x:Name="TxtLeftoverRegistry" Style="{StaticResource FieldTextBox}" TextWrapping="Wrap"/>

                            <TextBlock Style="{StaticResource SectionLabel}" Text="Reboot Behavior"/>
                            <CheckBox x:Name="ChkPromptReboot" Style="{StaticResource FeatureToggle}" Content="Prompt for reboot after installation"/>
                            <CheckBox x:Name="ChkForceReboot" Style="{StaticResource FeatureToggle}" Content="Force reboot (use with caution!)"/>
                        </StackPanel>
                    </TabItem>

                    <!-- Tab 7: UI -->
                    <TabItem Header="UI &amp; Timing">
                        <StackPanel Margin="15">
                            <TextBlock Style="{StaticResource SectionLabel}" Text="Installer UI Customization"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Banner Color (hex)"/>
                            <TextBox x:Name="TxtBannerColor" Style="{StaticResource FieldTextBox}"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Accent Color (hex)"/>
                            <TextBox x:Name="TxtAccentColor" Style="{StaticResource FieldTextBox}"/>
                            <CheckBox x:Name="ChkShowProgressBar" Style="{StaticResource FeatureToggle}" Content="Show progress bar during install"/>
                            <CheckBox x:Name="ChkSimulateDelay" Style="{StaticResource FeatureToggle}" Content="Simulate install delay"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Delay per step (ms)"/>
                            <TextBox x:Name="TxtDelayMs" Style="{StaticResource FieldTextBox}" Width="100" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </TabItem>

                </TabControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="15,10">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock FontSize="11" Foreground="#888">
                        <Run Text="TestPackage by "/>
                        <Hyperlink NavigateUri="https://rwksystems.com" RequestNavigate="Hyperlink_Navigate">RWK Systems</Hyperlink>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Load Config..." Padding="12,5" Margin="5,0" Click="LoadConfig_Click"/>
                    <Button Content="Save Config..." Padding="12,5" Margin="5,0" Click="SaveConfig_Click"/>
                    <Button x:Name="BtnGenerate" Content="Generate Installer" Padding="15,5" Margin="5,0"
                            Background="#0078D4" Foreground="White" BorderBrush="#0078D4"
                            FontWeight="SemiBold" Click="GenerateInstaller_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
