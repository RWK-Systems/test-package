<Window x:Class="TestPackageApp.UninstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestPackage - Uninstall" Height="450" Width="550"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#C62828">
            <TextBlock Text="TestPackage - Uninstall" FontSize="16" FontWeight="SemiBold"
                       Foreground="White" VerticalAlignment="Center" Margin="20,0"/>
        </Border>

        <!-- Content -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1">
            <Grid Margin="20,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Uninstalling TestPackage" FontSize="18" FontWeight="SemiBold"
                           Foreground="#1A1A1A" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="13" Foreground="#555" Margin="0,0,0,15">
                    The following actions will be performed to remove TestPackage from your system.
                </TextBlock>

                <ProgressBar Grid.Row="2" x:Name="UninstallProgress" Height="20" Margin="0,0,0,10"
                             Minimum="0" Maximum="100" Foreground="#C62828"/>

                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="UninstallLog" IsReadOnly="True" TextWrapping="Wrap"
                             FontFamily="Consolas" FontSize="11" Background="#1E1E1E"
                             Foreground="#CCCCCC" Padding="8" BorderThickness="0"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="15,0">
                <Button x:Name="BtnClose" Content="Close" Padding="15,5" Click="Close_Click"
                        IsEnabled="False"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
